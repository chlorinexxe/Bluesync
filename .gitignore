*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env

# debug.keystore is intentionally tracked (not ignored) - see app/build.gradle.kts's
# debugConfig signing config. It must be identical across every machine and CI run, or
# debug builds signed with different keys can't be installed over each other on a device.

# Allow APK files to be tracked in Git
!*.apk
